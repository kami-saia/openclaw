diff --git a/dist/agent-loop.js b/dist/agent-loop.js
index b26d45753809da14df6409354e8c537684b9acd7..6ceaa2ffd50e0b944633aa179221e437454f1a6b 100644
--- a/dist/agent-loop.js
+++ b/dist/agent-loop.js
@@ -103,6 +103,13 @@ async function runLoop(currentContext, newMessages, config, signal, stream, stre
                     currentContext.messages.push(result);
                     newMessages.push(result);
                 }
+                // Abort check: stop the loop if signal was aborted during tool execution
+                if (signal?.aborted) {
+                    stream.push({ type: "turn_end", message, toolResults });
+                    stream.push({ type: "agent_end", messages: newMessages });
+                    stream.end(newMessages);
+                    return;
+                }
             }
             stream.push({ type: "turn_end", message, toolResults });
             // Get steering messages after turn completes
@@ -209,6 +216,14 @@ async function executeToolCalls(tools, assistantMessage, signal, stream, getStee
     const results = [];
     let steeringMessages;
     for (let index = 0; index < toolCalls.length; index++) {
+        // Abort check: skip remaining tools if signal is aborted
+        if (signal?.aborted) {
+            const remaining = toolCalls.slice(index);
+            for (const skipped of remaining) {
+                results.push(skipToolCall(skipped, stream));
+            }
+            break;
+        }
         const toolCall = toolCalls[index];
         const tool = tools?.find((t) => t.name === toolCall.name);
         stream.push({
